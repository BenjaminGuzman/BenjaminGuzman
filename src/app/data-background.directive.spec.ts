import { DataBackgroundDirective } from './data-background.directive';

describe('DataBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new DataBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
